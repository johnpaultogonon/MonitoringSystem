VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# Optional: makes modal email changes apply immediately on login (dev server route /api/admin-commit-auth-profile).
# Supabase Dashboard → Project Settings → API → service_role (never expose in frontend / never use VITE_ prefix).
SUPABASE_SERVICE_ROLE_KEY=
